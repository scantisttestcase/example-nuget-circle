version: 2
jobs:
  build:
    docker:
      - image: jonathell/multi-manager:1.0

    steps:
      - checkout            
      - run:
          name: install dep
          command: |
            dotnet restore
