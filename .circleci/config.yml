version: 2
jobs:
  build:
    docker:
      - image: microsoft/dotnet-framework
    steps:
      - checkout            
      - run:
          name: install dep
          command: |
            dotnet build
