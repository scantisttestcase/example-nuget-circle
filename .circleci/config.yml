version: 2
jobs:
  build:
    docker:
      - image: mcr.microsoft.com/dotnet/framework/sdk

    steps:
      - checkout            
      - run:
          name: install dep
          command: |
            dotnet restore
