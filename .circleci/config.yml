version: 2
jobs:
  build:
    docker:
      - image: circleci/windows@1.0.0
    steps:
      - checkout            
      - run:
          name: install dep
          command: |
            dotnet build
